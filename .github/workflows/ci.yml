name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    uses: thecoreboys/coreboys-infra/.github/workflows/reusable-node-ci.yml@main
    with:
      node-version: "20"
      pnpm-version: "10"
      run-test: false
