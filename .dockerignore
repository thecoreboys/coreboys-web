.git
.github
.next
.next-*
node_modules
coverage
dist
out
.cache
.vercel
.clerk
certificates

.env
.env.*
!.env.example
*.log
*.exe
doctl-*

# The production image is intentionally self-contained. Member, crew, group,
# community, and brand artwork all ship from public/ so a missing media CDN
# can never turn the primary browse experience into broken image tiles.
public/house-reveal.mp4

# Local face-analysis data is private and never belongs in a web image.
workers/face-analyzer/.venv
workers/face-analyzer/models
workers/face-analyzer/authorized-media
workers/face-analyzer/authorized-enrollment
workers/face-analyzer/.local-data
workers/face-analyzer/config.local.json
**/__pycache__
**/*.pyc
