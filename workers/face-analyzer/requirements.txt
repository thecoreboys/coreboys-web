# Reproducible top-level versions. For deployment, generate requirements.lock with
# hashes on the target OS/architecture; see README.md. This file intentionally does
# not download or name any face-model weights.
av==15.1.0
numpy==2.2.6
opencv-python-headless==4.12.0.88
