"use client";

// Lightweight stub — the real implementation lives below in the AI pass.
// We only declare the symbol here so app/layout.tsx imports cleanly during
// the stylization commits. The next commit replaces this with the real UI.
export function Concierge() {
  return null;
}
