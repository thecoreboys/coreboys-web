/**
 * Site-wide footer intentionally removed. Keep this no-op export temporarily
 * so older page modules cannot accidentally bring the retired footer back.
 */
export function SiteFooter() {
  return null;
}
