"use client";
import { useState } from "react";
export function NotificationTestForm() { const [email, setEmail] = useState(""); const [status, setStatus] = useState(""); const [pending, setPending] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setPending(true); setStatus(""); try { const response = await fetch("/api/admin/notifications/test", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email }) }); const data = await response.json().catch(() => null); if (!response.ok) throw new Error(data?.error === "account_not_found" ? "Account not found." : "Couldn’t send the test alert."); setStatus("Test alert sent."); } catch (error) { setStatus(error instanceof Error ? error.message : "Couldn’t send the test alert."); } finally { setPending(false); } } return <form onSubmit={submit} className="rounded-2xl bg-secondary p-5 shadow-xl ring-1 ring-inset ring-secondary"><label className="block text-sm font-semibold text-primary" htmlFor="test-email">Fan email</label><input id="test-email" type="email" required value={email} onChange={(event) => setEmail(event.target.value)} className="mt-2 block w-full rounded-lg border border-secondary bg-primary px-3 py-2 text-sm text-primary outline-none focus:ring-2 focus:ring-brand-solid" placeholder="fan@example.com"/><button type="submit" disabled={pending} className="mt-4 rounded-lg bg-brand-solid px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{pending ? "Sending…" : "Send test alert"}</button>{status ? <p className="mt-3 text-sm text-tertiary" role="status">{status}</p> : null}</form>; }
